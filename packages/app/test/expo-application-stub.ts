/**
 * Stub de `expo-application` para los tests: es un módulo nativo y no carga bajo Vitest.
 * Devuelve una versión/build fijos para que `VersionFooter` renderice de forma estable.
 */
export const nativeApplicationVersion: string | null = '1.8.0';
export const nativeBuildVersion: string | null = '1';
